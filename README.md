click on https://emuohwo.github.io/API/UI/ to view the ui 

write "npm install" to install the required dependencies

write npm run build for compiled to es5

To start server, write:
npm run dev-start
